# Evals

Ten katalog jest miejscem na przyszłe oceny warstwy AI. `datasets/`, `schemas/` i `rubrics/` opisują odpowiednio wersjonowane przypadki, kontrakty danych i kryteria jakości. Evale nie zastępują testów deterministycznej domeny.
